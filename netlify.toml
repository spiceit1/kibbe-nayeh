[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["stripe", "resend", "twilio", "@supabase/supabase-js"]

[dev]
  command = "npm run dev"
  functions = "netlify/functions"
  targetPort = 5173

